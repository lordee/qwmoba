void (entity player, float menutype) ShowMenu = {
    /*msg_entity = player;
    WriteByte(MSG_MULTICAST, SVC_CGAMEPACKET); 
    WriteByte(MSG_MULTICAST, MNU_LOBBY); 
    multicast('0 0 0', MULTICAST_ONE); */
}